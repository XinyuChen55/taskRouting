# 第一周工作总结

第一周主要在熟悉课题内容、确认一些需要的信息，进行立项对齐，然后也开始收集一些开源数据集，并进行任务分类，描述每类任务的rubric、硬门槛和典型难例。更具体内容如下：

### 1. 测试内部LLM接口
我根据提供的公司内部LLM接口进行了调用，测试过程中发现提供的6个候选模型中，有五个会返回模型名称错误，只有 deepseek-v4-flash 可以正常返回结果，但返回结果的模型名称显示为 qwen3_6，询问了模型的相关负责人，了解到不同模型需要不同的 token ，所以后续还需要带教给我提供其他模型的接口，同时 deepseek-v4-flash 之前被下线了，接口后面的模型改为了 qwen3_6 ，所以实际使用模型是 qwen3_6 。

### 2. 测试OCR接口
在公司的电脑上测试能成功返回结果

### 3. 进行任务分类
我根据课题书的初始分类，用 Markdown 进行了任务分类，具体内容在 [任务分类评分标准](../D1_task_classification_scoring_critiria.md)，目前还没写完，预计今天完成。

### 4. 寻找开源测评数据集
由于内部用例不容易获取，我开始收集一些金融领域的带答案、标签或评分标准的数据集。

# 本周预计完成的工作
1. 完成P1阶段的任务分类
2. 整理候选数据集，收集更多英文和非金融领域的数据集，开始进行P2阶段的任务，整理 Golden Set ，筛选每类任务的样本。